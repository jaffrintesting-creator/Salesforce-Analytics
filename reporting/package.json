{
  "name": "qa-reporting",
  "version": "1.0.0",
  "description": "QA pipeline reporting tools: Playwright JSON -> SQLite -> CSV for Power BI",
  "scripts": {
    "build": "tsc",
    "export-results": "node export-results.js",
    "export-csv": "node export-to-csv.js --db qa.db --out ./csv-export"
  },
  "dependencies": {
    "sql.js": "^1.14.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/sql.js": "^1.4.11",
    "typescript": "^5.0.0"
  }
}
